# Telegram News Bot - FAQ и Шпаргалка

## ❓ Часто задаваемые вопросы

### 1. Как получить TOKEN бота?

**Шаг 1**: Откройте Telegram
**Шаг 2**: Найдите и откройте чат с [@BotFather](https://t.me/botfather)
**Шаг 3**: Отправьте `/newbot`
**Шаг 4**: Следуйте инструкциям:
- Выберите название (видно пользователям)
- Выберите username (должен заканчиваться на `_bot`)

**Шаг 5**: Вы получите **TOKEN** - сохраните его!

Пример: `123456789:ABCdefGHIjklmnOpqrsTUVwxyZ-1234567890`

---

### 2. Как получить ID канала?

**Способ 1** (Быстро):
1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в личный чат
2. Отправьте `/start`
3. Ваш ID будет показан

**Способ 2** (Для канала):
1. Добавьте [@userinfobot](https://t.me/userinfobot) в ваш канал
2. Отправьте в канал сообщение
3. Бот ответит с ID канала

**Способ 3** (Через ссылку):
- Обычно ID = -100 + цифры из ссылки канала
- Пример: канал `t.me/c/1234567890/1` → ID = `-1001234567890`

⚠️ **Важно**: ID каналов в JSON ВСЕГДА как ЦЕЛОЕ ЧИСЛО в квадратных скобках:
```json
["1001234567890"]
```

---

### 3. Где взять RSS feed?

**Встроенные RSS каналы** (есть почти везде):
- Часто: `https://example.com/feed` или `/rss`
- Правой кнопкой на странице → "Код страницы" → найти `<link rel="alternate" type="application/rss+xml"`

**Популярные источники**:
```
Habr: https://habr.com/ru/rss/all/
GitHub: https://github.blog/feed/
Medium: https://medium.com/feed/
YouTube: https://www.youtube.com/feeds/videos.xml?channel_id=CHANNEL_ID
Twitter (через Nitter): https://nitter.net/USERNAME/rss
```

**Проверка валидности**:
- Откройте RSS в браузере
- Если видите XML с `<rss>` или `<feed>` - валиден ✅
- Используйте [Feed Validator](https://validator.w3.org/feed/)

---

### 4. Как добавить Дзен?

1. Откройте канал Дзена: `dzen.ru/s/YOUR_CHANNEL_NAME`
2. Используйте шаблон:
   ```
   https://dzen.ru/feed/rss/?channel_name=YOUR_CHANNEL_NAME
   ```

3. Пример:
   - Ссылка: `dzen.ru/s/crypto_news`
   - RSS: `https://dzen.ru/feed/rss/?channel_name=crypto_news`

---

### 5. Как добавить X/Twitter?

Бот использует **Nitter** (работает везде, нет API ключей):

```
Просто введите username:
- elonmusk
- sama
- vitalikbuterin

Бот автоматически:
- Конвертирует в: https://nitter.net/USERNAME/rss
- Получает публичные твиты
- Избегает блокировок
```

**Ограничения**:
- Только публичные твиты
- Без ретвитов и лайков
- Только текст и ссылки

---

### 6. Бот не постит новости?

**Проверьте:**

```bash
# 1. Бот запущен?
systemctl status newsbot

# 2. Логи есть?
sudo journalctl -u newsbot -f

# 3. Ручной тест:
```

В чате с ботом отправьте (если вы администратор):
```
/fetch
```

**Частые ошибки:**

| Ошибка | Решение |
|--------|---------|
| `Chat not found` | ID канала неверный, проверьте формат: -100... |
| `Unauthorized` | Бот не добавлен в канал как администратор |
| `Feed parsing failed` | URL источника неверный или недоступен |
| `Timeout` | Источник долго отвечает, попробуйте другой |

---

### 7. Как изменить расписание?

**Файл**: `advanced_bot.py`, метод `setup_schedule()`

```python
# Текущее: каждые 30 минут
scheduler.add_job(
    self.fetch_news_job,
    CronTrigger(minute='*/30'),  # Измените число
    id='fetch_every_30min'
)
```

**Примеры**:
```python
CronTrigger(minute='*/15')         # Каждые 15 минут
CronTrigger(minute='*/60')         # Каждый час
CronTrigger(hour='*/4', minute='0') # Каждые 4 часа
CronTrigger(hour='9,17', minute='0') # В 9 и 17 часов
CronTrigger(day_of_week='0-4', hour='9') # Будни в 9:00
```

---

### 8. Как видеть логи?

```bash
# systemd
sudo journalctl -u newsbot -f           # Real-time
sudo journalctl -u newsbot --lines 50   # Последние 50 строк
sudo journalctl -u newsbot --since "1 hour ago"

# Docker
docker logs -f newsbot                  # Real-time
docker logs --tail 100 newsbot          # Последние 100 строк

# Файл (если логируете в файл)
tail -f /var/log/newsbot.log
```

---

### 9. Как обновить бота?

```bash
# 1. Остановить
sudo systemctl stop newsbot

# 2. Скачать новые файлы
cd /home/newsbot/telegram-news-bot
git pull  # или скопировать новые файлы

# 3. Обновить зависимости
source venv/bin/activate
pip install --upgrade -r requirements.txt

# 4. Запустить
sudo systemctl start newsbot
```

---

### 10. Как удалить источник?

В чате с ботом (администратор):
```
/remove_source
```

Или прямо в БД:
```bash
sqlite3 news_bot.db
UPDATE sources SET active = 0 WHERE name = 'Habr';
.quit
```

---

## 🚀 Шпаргалка по командам

### Администратору

```
/start              - Приветствие
/help               - Справка
/add_source         - Добавить источник
/remove_source      - Удалить источник
/sources            - Список источников
/fetch              - Получить новости сейчас
```

### Интерпретация ошибок

```
❌ "TELEGRAM_BOT_TOKEN not set" 
   → Проверьте .env файл

❌ "Chat not found" 
   → ID канала неверный

❌ "Unauthorized" 
   → Бот не администратор в канале

⚠️ "Feed parsing failed" 
   → Источник недоступен (попробуйте позже)

⚠️ "Malformed XML" 
   → Нормально, feedparser справляется

✅ Стартап успешен, если видите: "🚀 Бот запущен!"
```

---

## 🔧 Шпаргалка по конфигурации

### Структура проекта

```
telegram-news-bot/
├── news_bot.py           # Основной код
├── advanced_bot.py       # С scheduler
├── config_examples.py    # Готовые источники
├── .env                  # Переменные (НЕ коммитить!)
├── .env.example          # Шаблон
├── .gitignore           # Git исключения
├── requirements.txt     # Зависимости
├── Dockerfile           # Docker конфигурация
├── README.md            # Документация
├── DEPLOYMENT.md        # Гайд развертывания
└── news_bot.db          # База данных (создается автоматически)
```

### .env файл

```env
# Обязательно
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
ADMIN_ID=987654321
TELEGRAM_CHANNELS=[-1001234567890]

# Опционально
LOG_LEVEL=INFO
DATABASE_FILE=news_bot.db
```

### requirements.txt

```
aiogram==3.5.0
feedparser==6.0.10
aiohttp==3.9.3
python-dotenv==1.0.1
apscheduler==3.11.0
```

---

## 📊 SQL Запросы для мониторинга

```sql
-- Все источники
SELECT id, name, type, active FROM sources;

-- Количество опубликованных новостей
SELECT COUNT(*) FROM published_news;

-- Последние 10 новостей
SELECT title, published_at FROM published_news ORDER BY published_at DESC LIMIT 10;

-- Источники с наибольшим количеством новостей
SELECT s.name, COUNT(*) as count 
FROM published_news pn 
JOIN sources s ON pn.source_id = s.id 
GROUP BY pn.source_id 
ORDER BY count DESC;

-- Удалить все опубликованные новости (осторожно!)
DELETE FROM published_news;
```

---

## 🔐 Безопасность - Checklist

- [ ] `.env` файл в `.gitignore`
- [ ] Токен не опубликован в интернете
- [ ] ADMIN_ID установлен правильно
- [ ] Бот добавлен в каналы как администратор
- [ ] Используются HTTPS источники где возможно
- [ ] Регулярно проверяются логи на ошибки
- [ ] Создаются резервные копии БД
- [ ] Используются разные токены для dev/prod

---

## 💾 Резервное копирование

```bash
# Создать backup
cp news_bot.db news_bot.db.backup.$(date +%Y%m%d_%H%M%S)

# Восстановить из backup
cp news_bot.db.backup.20240115_120000 news_bot.db

# Автоматический ежедневный backup (cron)
# Добавьте в: crontab -e
0 2 * * * cp /home/newsbot/telegram-news-bot/news_bot.db /backup/news_bot.db.$(date +\%Y\%m\%d)
```

---

## 📈 Оптимизация производительности

```python
# Если много источников (>50):

# 1. Используйте Redis для кэша
from redis import Redis
cache = Redis(host='localhost')

# 2. Параллельная обработка
import asyncio
tasks = [parse_source(s) for s in sources]
results = await asyncio.gather(*tasks)

# 3. Увеличьте интервал между fetch
CronTrigger(minute='*/60')  # Вместо */30

# 4. Используйте PostgreSQL вместо SQLite
```

---

## 🐛 Debugging

```python
# Включить подробное логирование
import logging
logging.basicConfig(level=logging.DEBUG)

# Проверить рабочий RSS
import feedparser
feed = feedparser.parse('https://habr.com/ru/rss/all/')
print(f"Entries: {len(feed.entries)}")

# Проверить Telegram API
from aiogram import Bot
bot = Bot(token=TOKEN)
await bot.get_me()  # Если успех - токен валиден
```

---

## 🎯 Tipы и трюки

1. **Быстрый старт**: Используйте готовые presets из `config_examples.py`
2. **Тестирование**: Используйте `/fetch` перед активацией scheduler
3. **Мониторинг**: Включите еженедельные отчеты
4. **Масштабирование**: Начните с 5-10 источников, добавляйте по мере необходимости
5. **Обновления**: Проверяйте aiogram, feedparser на обновления каждый месяц

---

## 📞 Где искать помощь

- **Telegram Bot API**: https://core.telegram.org/bots/api
- **aiogram docs**: https://docs.aiogram.dev/
- **feedparser docs**: https://feedparser.readthedocs.io/
- **Проблемы**: Создавайте issues на GitHub
- **Вопросы**: Комьюнити Telegram разработчиков

---

## 🎓 Дополнительное обучение

После освоения базика:

1. **Добавьте веб-интерфейс** (Flask/FastAPI)
2. **Интегрируйте с СУБД** (PostgreSQL)
3. **Используйте Redis** для кэша
4. **Добавьте фильтрацию новостей** (по ключевым словам)
5. **Настройте отправку в несколько каналов** (с разными фильтрами)
6. **Используйте API источников** (где доступны)
7. **Добавьте NLP анализ** (категоризация, тональность)
